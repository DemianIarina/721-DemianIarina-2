/*
package com.company;

import com.company.repository.BestellungRepository;

import java.util.ArrayList;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class ControllerTest {

    @org.junit.jupiter.api.Test
    void sortierenBestellungen() {
        BestellungRepository bestellungRepository = new BestellungRepository();
        bestellungRepository.create(new Bestellung(1, "Adresse1", List.of(new Produkt("Produkt1", 10), new Produkt("Produkt2", 12))));
        bestellungRepository.create(new Bestellung(2, "Adresse1", List.of(new Produkt("Produkt3", 17), new Produkt("Produkt2", 12))));

        Controller controller = new Controller(bestellungRepository);
        controller.sortierenBestellungen();

        List<Bestellung> expectedBestellungen;

        //assert
    }

    @org.junit.jupiter.api.Test
    void sortNachProdukt() {
    }
}*/
